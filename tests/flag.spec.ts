import { rest, test } from './__mocks__/global'
test.describe('flag page', () => {
  // should display history
  // should allow updating name
  // should allow updating description
  // should allow updating tags
  // should verify that browser title includes project and flag
})
